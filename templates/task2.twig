{% extends "base.html.twig" %}


{% block body %}
    <table>
        <tr>
            <th>id</th>
            <th>nome</th>
            <th>cognome</th>
        </tr>
        {% for item in user %}
            <tr>
                <td>{{ item.id }}<td>
                <td>{{ item.nome }}<td>
                <td>{{ item.cognome }}<td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}

